name: image
on:
  pull_request:
  push:
    branches:
      - master
      - release/**

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build-image:
    strategy:
      matrix:
        arch: [amd64, arm64]

    runs-on: |-
      ${{fromJson('{
        "amd64": "ubuntu-20.04",
        "arm64": "ubuntu-22.04-arm64-relay"
      }')[matrix.arch] }}

    env:
      # IMG_CACHE: ghcr.io/getsentry/symbolicator:${{ matrix.arch }}-latest
      IMG_VERSIONED: ghcr.io/getsentry/symbolicator:${{ matrix.arch }}-${{ github.sha }}
      NIGHTLY_IMG_CACHE: ghcr.io/getsentry/symbolicator:${{ matrix.arch }}-nightly
      BUILDER_IMG_CACHE: ghcr.io/getsentry/symbolicator:${{ matrix.arch }}-builder

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: build builder img
        run: |
          set -euxo pipefail
          args=()
          if docker pull -q "$BUILDER_IMG_CACHE"; then
            args+=(--cache-from "$BUILDER_IMG_CACHE")
          fi
          docker buildx build \
              "${args[@]}" \
              --build-arg BUILDKIT_INLINE_CACHE=1 \
              --platform linux/${{ matrix.arch }} \
              --tag "$BUILDER_IMG_CACHE" \
              --target symbolicator-build \
              .

      - name: build nightly img
        run: |
          set -euxo pipefail
          args=()
          if docker pull -q "$NIGHTLY_IMG_CACHE"; then
            args+=(--cache-from "$NIGHTLY_IMG_CACHE")
          fi
          docker buildx build \
              "${args[@]}" \
              --build-arg BUILDKIT_INLINE_CACHE=1 \
              --platform linux/${{ matrix.arch }} \
              --tag "$NIGHTLY_IMG_CACHE" \
              --tag "$IMG_VERSIONED" \
              --cache-from "${BUILDER_IMG_CACHE}" \
              --cache-from "${NIGHTLY_IMG_CACHE}" \
              .

      - name: push all images
      #if: github.event_name != 'pull_request'
        run: |
          set -euxo pipefail
          docker login --username '${{ github.actor }}' --password '${{ secrets.GITHUB_TOKEN }}' ghcr.io
          docker push "$IMG_VERSIONED"


  assemble:
    # needs: [build-image]

    runs-on: ubuntu-20.04

    # if: github.event_name != 'pull_request'

    steps:
      - name: Assemble multi arch image
        run: |
          set -euxo pipefail
          docker login --username '${{ github.actor }}' --password '${{ secrets.GITHUB_TOKEN }}' ghcr.io

          # docker manifest create \
          #   'ghcr.io/getsentry/symbolicator:${{ github.sha }}' \
          #   'ghcr.io/getsentry/symbolicator:arm64-${{ github.sha }}' \
          #   'ghcr.io/getsentry/symbolicator:amd64-${{ github.sha }}'

          docker manifest create \
            'ghcr.io/getsentry/symbolicator:e980fa196a84f528069c57f57d63cb736d07ee28' \
            'ghcr.io/getsentry/symbolicator:arm64-e980fa196a84f528069c57f57d63cb736d07ee28' \
            'ghcr.io/getsentry/symbolicator:amd64-e980fa196a84f528069c57f57d63cb736d07ee28'

          # docker manifest push ghcr.io/getsentry/symbolicator:${{ github.sha }}
          docker manifest push ghcr.io/getsentry/symbolicator:e980fa196a84f528069c57f57d63cb736d07ee28
