`sentry_example.sym` is a breakpad file with present, but severely truncated CFI.
`sentry_example.sym.original` is the untruncated version. It was extracted from a
MacOS dSYM file using breakpad's `dump_syms`.
